name: autoencoder
description: "Preprocess abundances for autoencoder training: load, scale, and save as PyTorch tensors"

# Input configuration
input:
  type: preprocessing
  source: initial

# Output files
output:
  files:
    - autoencoder_train_preprocessed.pt
    - autoencoder_val_preprocessed.pt

# Dataset config via composition
defaults:
  - /data: grav
