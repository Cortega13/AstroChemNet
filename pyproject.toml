[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["AstroChemNet"]
package-dir = {"" = "src"}

[project]
name = "AstroChemNet"
version = "0.1.0"
description = "A surrogate model for UCLCHEM."
readme = "README.md"
requires-python = ">=3.7"
dependencies = []